[project]
name = "spendingsApp"
version = "0.1.0"
description = ""
authors = [
    {name = "Eetu Huttula",email = ""}
]
license = {text = "MIT"}
requires-python = "^3.12"
dependencies = [
]

[dependency-groups]
dev = [
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
